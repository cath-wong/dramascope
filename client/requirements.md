## Packages
papaparse | Required for parsing CSV files client-side
@types/papaparse | TypeScript definitions for papaparse

## Notes
- CSV files should be placed in the `/public` directory.
- App handles missing files gracefully and instructs user to upload them.
