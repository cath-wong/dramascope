READ CAREFULLY BEFORE MAKING ANY CODE CHANGES.

The current codebase state is authoritative, including:
- completed Step 9.4A and 9.4B
- all manual refinements made by the user after Step 9.4B
- completed Step 10.1–10.7
- the current Discursive Concepts tab layout and behaviour

CRITICAL PROTECTION RULES:

1. Do NOT rewrite, refactor, simplify, reorganise, or restyle any existing working component unless required by this step.

2. Do NOT undo or overwrite manual edits made by the user after Step 9.4B.

3. Treat the current user-edited code as the single source of truth.

4. Before making changes, list exactly which files will be edited and why.

5. Prefer creating a new utility file instead of expanding Analysis.tsx heavily.

6. Do NOT modify these files unless absolutely necessary:
   - client/src/components/D3Sankey.tsx
   - client/src/utils/sankey.ts

7. If Analysis.tsx must be edited, keep the change minimal and local.

8. Do NOT alter or remove any existing panels including:
   - Sankey panel
   - Quad Inventory
   - Top Quads
   - Temporal Concept Flow
   - Core vs Peripheral Quads
   - Constellation Similarity / Distance
   - Constellation Similarity Matrix

9. Do NOT change quad extraction logic or similarity calculations.

10. Stop and ask if any step risks changing existing behaviour.

First respond with:
A. proposed file list
B. reason for each edit
C. confirmation existing behaviour will not change

Wait for approval before editing.

--------------------------------------------------

STEP 10.8 — Constellation Clustering

Goal:
Add a simple, transparent clustering layer that groups node constellations using the similarity matrix already produced in Step 10.7.

Conceptual definition:
A constellation cluster is a group of node lemmas whose constellation similarity exceeds a defined threshold.

Do NOT change:
- quad extraction
- Sankey logic
- similarity matrix calculations
- pairwise comparison logic
- Discursive pipeline

This step only interprets the existing matrix.

--------------------------------------------------

1. Input

Use the existing similarity matrix from Step 10.7.

Matrix measure:
Quad Jaccard similarity (already implemented).

--------------------------------------------------

2. Clustering rule

Implement a simple threshold-based clustering rule:

Two nodes belong to the same cluster if:

similarity ≥ threshold

Default threshold:
0.02 (2%)

Allow user adjustment via dropdown:
- 1%
- 2%
- 5%

No complex ML clustering yet.

--------------------------------------------------

3. Cluster computation

Group nodes into clusters based on similarity connectivity.

Example output:

Cluster 1
lord
sir
good

Cluster 2
love
man
know

Cluster 3
stage
here
there

Also compute:

cluster size
average internal similarity

--------------------------------------------------

4. Panel

Add a new Discursive panel titled:

Constellation Clusters

Panel contents:

Cluster ID
Members
Cluster size
Average internal similarity

Allow CSV export.

--------------------------------------------------

5. Interaction

Optional but useful:

Clicking a node inside a cluster should:
- populate the active node field
- allow immediate exploration via existing panels.

Do NOT break the existing workflow.

--------------------------------------------------

6. Guardrails

If matrix is too small to cluster:

Display message:
"No clusters available under current settings."

Do NOT crash the panel.

--------------------------------------------------

7. Acceptance tests

- existing panels remain unchanged
- similarity matrix still works
- pairwise comparison still works
- cluster panel renders correctly
- clusters change when threshold changes
- no change to quad counts or constellation extraction

Stop after Step 10.8 is implemented.