Project Log — Step 9.4C Deferred

Status:
Step 9.4C (Diachronic Heatmap) was attempted after Step 9.4B but has been deferred.

Reason for deferral:
Although a first-pass heatmap was generated, the current form behaves more like a diachronic neighbour-frequency table than a fully representative LDNA-style concept heatmap. The rows currently reflect individual neighbouring lemmas derived from quads, which is methodologically related to discursive concept modelling but not yet the strongest representation of quad-based conceptual structure.

Decision:
Do not implement or integrate Step 9.4C further at this stage of prototype development.

Authoritative current state:
The current app state after Step 9.4B, including all subsequent manual refinements made by the user, is now the authoritative baseline and must not be altered by future agent actions unless explicitly requested.

Future direction for 9.4C:
If revisited later, the heatmap should remain in the Discursive Concepts tab, not be moved to Lexical, because it derives from the speeches-based discursive pipeline rather than the lines-based lexical pipeline.

Preferred later refinement path:
1. Reframe first-pass version explicitly as a Diachronic Neighbour Heatmap if needed.
2. Consider ordering rows by temporal behaviour (for example strongest slice or first peak) rather than only total frequency.
3. Design a more LDNA-faithful later version using top quads or concept signatures rather than isolated neighbouring lemmas.
4. Treat colour scale, sticky headers, and related table polish as global UI refinements to be handled later across the app rather than within Step 9.4C alone.

Instruction for future development:
Do not revisit Step 9.4C automatically. Only return to it when explicitly requested.



READ CAREFULLY BEFORE MAKING ANY CODE CHANGES.

The current codebase state is authoritative, including:
- the completed Step 9.4A and 9.4B implementation
- all manual refinements made by the user after Step 9.4B
- the current layout and behaviour of the Discursive tab
- the current Sankey, Quad Inventory, Node Lemma panel, and related controls

CRITICAL PROTECTION RULES:

1. Do NOT rewrite, refactor, simplify, reorganise, or restyle any existing working component unless I explicitly request that exact file or panel to be changed.

2. Do NOT undo or replace any manual edits made after the original agent implementation.

3. Treat the current user-edited version as the single source of truth.

4. Before making changes, list exactly which files you plan to edit.

5. Default rule: create a NEW component or utility file instead of modifying existing core files.

6. Do NOT modify these core files unless explicitly authorised:
   - client/src/pages/Analysis.tsx
   - client/src/components/D3Sankey.tsx
   - client/src/utils/sankey.ts

7. If a task requires touching a core file, keep the edit minimal and local, and explain why it is necessary before changing it.

8. Do NOT change routing, tabs, panel order, shared state logic, or existing filtering behaviour unless explicitly requested.

9. Do NOT remove or alter any existing panel, including:
   - Node Lemma panel
   - Sankey panel
   - Quad Inventory
   - drift/stability panels

10. If there is any risk of affecting existing behaviour, stop and ask first.


Required working method:

First show:
A. files to be edited
B. reason each file must be edited
C. whether a new component can be created instead

Then wait for approval before changing code.

Do not implement anything yet.

First respond with:
1. proposed file list
2. change scope
3. confirmation that existing user-refined behaviour will be preserved